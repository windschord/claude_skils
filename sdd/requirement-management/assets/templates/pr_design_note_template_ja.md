# [PRタイトル]

## 対象要求

| 要求ID | 要求文 | このPRでの扱い |
|--------|--------|---------------|
| REQ-XXXX | [要求文] | 新規実装 / 変更 / 検証追加 |

## 前提の確認

**明示された情報**: [要求として確定している内容]

**不明な情報**: [なし / 項目と確認質問]

> 不明な情報が1つでも残っている場合、設計と実装に進まない。要求側を先に確定させる。

## 設計ノート

> このセクションが設計の唯一の記録です。リポジトリには永続化しません。

### 採用した方式

[データ構造、API形状、処理フローを簡潔に。図が必要なら Mermaid で書く]

### 検討した代替案と却下理由

| 代替案 | 却下理由 |
|--------|---------|
| [案] | [なぜ採らなかったか] |

### 実装上のトレードオフ

[何を犠牲にして何を取ったか]

## 要求充足の確認

- [ ] 対象要求の `verification` に本PRのテストを登録した
- [ ] 採用している実行パスの `reqctl.py` で `validate --strict --check-tests` を実行し、エラー・警告ゼロ
- [ ] `generated/` を再生成し、手編集していない
- [ ] 実装過程で要求の矛盾・不足を見つけた場合、要求側を先に直した
- [ ] 設計内容をレジストリのYAMLに書いていない

## 要求への影響

- 要求の変更: [なし / REQ-XXXX を変更（別途承認済み）]
- `design_refs` への追加: [このPRのURL]
